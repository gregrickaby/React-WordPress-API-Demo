import styled from "styled-components";

export const PostLayout = styled.article`
  .post-title {
    font-family: serif;
    font-size: 48px;
    line-height: 52px;
    margin-bottom: 30px;
  }

  .post-content {
    p {
      font-size: 18px;
      margin: 25px 0;
    }
  }
`;
